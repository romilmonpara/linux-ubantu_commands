# 🎉 Fun Linux Commands to Try (Ubuntu / WSL)

Most commands need a one-time installation using `sudo apt install <package>`.

---

## 🚂 sl — Steam Locomotive

```bash
sudo apt install sl -y
sl
```
Use case: fun animation when you mistype `ls`

---

## 🐄 cowsay — Talking Cow

```bash
sudo apt install cowsay -y
cowsay "Your message here"
fortune | cowsay
```
Use case: ASCII cow with messages or random quotes

---

## 🟩 cmatrix — Matrix Effect

```bash
sudo apt install cmatrix -y
cmatrix
```
Use case: Matrix-style terminal animation

---

## 🔥 aafire — Terminal Fire

```bash
sudo apt install libaa-bin -y
aafire
```
Use case: fire animation in terminal

---

## 🔮 fortune — Random Quotes

```bash
sudo apt install fortune -y
fortune
```
Use case: display random quotes

---

## 🔤 figlet — ASCII Text Banner

```bash
sudo apt install figlet -y
figlet "Your Text"
```
Use case: generate ASCII banners

---

## 🔊 espeak — Text to Speech

```bash
sudo apt install espeak -y
espeak "Hello, WSL user"
```
Use case: computer speaks text

---

## 🔁 rev — Reverse Text

```bash
echo "hello world" | rev
```
Use case: reverse text

---

## ♾️ yes — Endless Output

```bash
yes "Keep going!"
```
Use case: infinite output (Ctrl+C to stop)

---

## 🐱 oneko — Desktop Pet (GUI Required)

```bash
sudo apt install oneko -y
oneko
oneko -dog
```
Use case: cat or dog chases mouse cursor
